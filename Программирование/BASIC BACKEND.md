## Сервер
 это обычный компьютер, на котором запущена программа. Кстати, это может быть и не один компьютер, а несколько связанных, тогда это будет распределённая система. Распределённая система состоит из нескольких компьютеров, каждый из которых выполняет определенные функции и обменивается информацией с другими компьютерами в сети. В распределённой системе каждый компьютер называется узлом, а все узлы работают вместе, чтобы предоставить необходимые ресурсы и услуги пользователям.
## DNS (Domain Name System)
Это система, которая переводит доменные имена в IP-адреса.  DNS — это распределённая база данных, которая хранит соответствия между доменным именем и IP-адресом.![[Снимок экрана 2025-09-01 в 11.15.36.png]]

## IP-адрес
 это числовой идентификатор устройства в сети. У любого компьютера, который подключен к сети, есть IP адрес. IPv4 адреса состоят из 4 чисел, разделенными точками. Например: 127.0.0.1 - это адрес вашего локального компьютера, более широко известный по своему имени — localhost.

Чтобы несколько программ могли одновременно работать на одном компьютере, все программы запускаются на разных портах. Одно приложение может работать только на одном порте.

Итак, DNS предоставляет клиенту IP-адреc, например, 12.35.11.52. Затем нужно по порту найти нужную программу, запущенную на сервере. Так, например, нужная программа будет работать по адресу: 12.35.11.52:8080 — на этот адрес клиент отправляет HTTP-запрос.

#### Общая схема работы (от пользователя к сайту)

1. **Запрос DNS:** Браузер получает от DNS IP-адрес сервера, на котором находится сайт.
    
2. **Установление соединения (TCP):** Браузер использует IP и порт для установки надежного TCP-соединения с сервером.
    
3. **Отправка HTTP-запроса:** После установления соединения браузер отправляет HTTP-запрос (например, "покажи главную страницу") на конкретный порт веб-сервера (например, 80 для HTTP или 443 для HTTPS).
    
4. **Обработка запроса:** Программа (веб-сервер, например, Nginx или Apache), "слушающая" на том порту, получает запрос, обрабатывает его и формирует ответ.
    
5. **Получение ответа:** Браузер получает HTTP-ответ (HTML, CSS, JS) и отображает страницу.
[[API COMMUNICATIONS#HTTP]]

## Linux basic commands
#### **Доступ и безопасность**

🔐 **SSH (Secure Shell)**

- Обеспечивает безопасный удаленный доступ к Linux-хостам.
    
- Команда: `ssh имя_пользователя@ip_адрес`
    
- Далее следует аутентификация по паролю.
    

👤 **Создание пользователя с правами sudo**

- Добавление нового пользователя: `sudo adduser имя_пользователя`
    
- Предоставление прав sudo: `sudo usermod -aG sudo имя_пользователя` (добавляет пользователя в группу `sudo`).
    

🔑 **Изменение прав владения файлами**

- Смена владельца и группы: `chown пользователь:группа имя_файла`
    
- Смена только группы: `chgrp группа имя_файла`
    

---

#### **Управление файлами**

🔗 **Символические ссылки**

- Создание: `ln -s исходный_файл имя_ссылки`
    
- Удаление: `rm имя_ссылки` (оригинальный файл не затрагивается).
    

📁 **Базовые операции с файлами**

- Копирование: `cp источник назначение`
    
- Перемещение/переименование: `mv источник назначение`
    
- Удаление: `rm имя_файла`
    

🗑️ **Необратимое удаление**

- Полное затирание файла: `shred имя_файла` (файл становится невосстановимым).
    
- Рекурсивное удаление каталога: `rm -rf имя_каталога` (удаляет папку и всё её содержимое).
    

📝 **Создание файлов**

- Создание пустого файла: `touch test.txt`
    

---

#### **Содержимое файлов и поиск**

📖 **Просмотр содержимого файла**

- Команда `cat` выводит всё содержимое файла.
    
- Полезно для просмотра небольших файлов или объединения нескольких файлов.
    

🔍 **Поиск текста в файлах**

- Эффективный поиск выполняется командой `grep`.
    
- Отображает строки и их номера, содержащие указанный текст.
    

---

#### **Системные и сетевые утилиты**

🌐 **Проверка сетевого подключения**

- Команда `ping` проверяет доступность хоста в сети и измеряет время отклика.
    

📊 **Мониторинг ресурсов системы**

- Мониторинг в реальном времени: команды `top` или `htop`.
    
- Показывают статистику по использованию CPU, памяти и запущенным процессам.
    

🔄 **Управление пакетами**

- Установка, обновление и удаление программного обеспечения.
    
- В Debian/Ubuntu: `apt-get` (например, `sudo apt-get install имя_пакета`).
    
- В RHEL/CentOS: `yum` (например, `sudo yum install имя_пакета`).

## CI CD
Continuous integration - набор практик для автоматизации постоянной интеграции новых изменений в основной репозиторий.

Continuous deployment - набор практик для автоматизации постоянного деплоя новый версий приложения

## Clouds
ЭТо просто такие же компьютеры, но которые находятся в чьем то датацентре. Я могу такие сервера арендовать на время, чтобы использовать их ресурсы. А их владельцы позаботятся об обслуживании и поддержке.

## Docker
Специальная программа, позволяющая быстро устанавливать и затем управлять всеми внешними зависимостями моих приложений.

Контейнер - маленькая и легковесная ОС вместе с только необходимыми программами, нужными для запуска того или иного приложения.
 ![[Снимок экрана 2025-09-02 в 15.51.09.png]]
 Образы - исходное описание, структура, из которой создаются контейнеры определенного наполнения. Это собрание запускаторов всех необходимых программ и алгоритм запуска этих программ для старта конкретного контейнера
 ![[Снимок экрана 2025-09-02 в 15.52.18.png]]
 
![[Снимок экрана 2025-09-02 в 15.54.44.png]]

Dockerfile - специальный текстовый файл, в котором описывается, из чего и как именно строится новый Docker-образ. Описывается это с помощью специальных слов-команд, доступных в документации докера. 
![[Снимок экрана 2025-09-02 в 15.56.54.png]]

docker-compose - специальная штука для оркестрации(управлением контейнерами)

## k8
 Kubernetes (K8s) — это открытая система для автоматизации развертывания, масштабирования и управления контейнеризированными приложениями. Он предоставляет платформу для оркестрации контейнеров, обеспечивая управление и автоматизацию контейнеризированными приложениями в динамически изменяющихся средах.

Принципы работы Kubernetes основаны на следующих ключевых концепциях:

1. Контейнеризация: Kubernetes основан на использовании контейнеров, таких как Docker. Контейнеры обеспечивают стандартизацию окружения выполнения приложений и их зависимостей, что облегчает переносимость приложений между различными средами.

2. Кластер: Kubernetes управляет группой серверов (узлов), объединенных в кластер. Кластер включает в себя мастер-узел (control plane) и рабочие узлы (nodes), где развертываются контейнеры. Мастер-узел управляет и контролирует всеми операциями в кластере.

3. Мастер-узел (Control Plane): Мастер-узел управляет кластером и принимает решения о том, какие контейнеры размещать, где их размещать и как поддерживать их состояние. Компоненты мастер-узла включают API Server, Scheduler, Controller Manager и etcd (распределенное хранилище состояния).

4. Рабочий узел (Node): Рабочий узел - это физический или виртуальный сервер, на котором работают контейнеры. Каждый узел имеет агент Kubernetes, который управляет контейнерами на этом узле и связывает их с мастер-узлом.

5. Pod: Контейнеры разворачиваются внутри объекта, называемого "Pod". Под является минимальной единицей развертывания в Kubernetes и может содержать один или несколько контейнеров, которые совместно используют одно пространство имен и сеть.

6. Управление ресурсами и масштабирование: Kubernetes позволяет определять ресурсы (CPU, память) для контейнеров и масштабировать приложения в зависимости от нагрузки. Он автоматически масштабирует количество реплик (копий) приложения в ответ на изменения требований.

7. Службы: Службы в Kubernetes предоставляют стабильные и постоянные точки доступа к одной или нескольким репликам приложения. Службы обеспечивают балансировку нагрузки и позволяют коммуникации между различными частями приложения в кластере.

8. Хранилище данных: Kubernetes предоставляет абстракцию для хранения данных с помощью объектов PersistentVolume и PersistentVolumeClaim. Это позволяет приложениям сохранять и восстанавливать свое состояние даже после перезапуска контейнеров.

9. Контроллеры и деплойменты: Контроллеры в Kubernetes отвечают за поддержание желаемого состояния системы. Деплойменты предоставляют декларативный способ описания требуемого состояния приложения и его обновлений.

